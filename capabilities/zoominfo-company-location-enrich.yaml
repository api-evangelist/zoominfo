naftiko: 1.0.0-alpha2
info:
  label: ZoomInfo — Company Location Enrich
  description: 'ZoomInfo — Company Location Enrich. 3 operations. Lead operation: Zoominfo Location Enrich. Self-contained
    Naftiko capability covering one Zoominfo business surface.'
  tags:
  - Zoominfo
  - Company Location Enrich
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ZOOMINFO_API_KEY: ZOOMINFO_API_KEY
capability:
  consumes:
  - type: http
    namespace: zoominfo-company-location-enrich
    baseUri: https://api.zoominfo.com
    description: ZoomInfo — Company Location Enrich business capability. Self-contained, no shared references.
    resources:
    - name: enrich-location
      path: /enrich/location
      operations:
      - name: locationenrich
        method: POST
        description: Zoominfo Location Enrich
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: lookup-inputfields-location-enrich
      path: /lookup/inputfields/location/enrich
      operations:
      - name: locationenrichinputs
        method: GET
        description: Zoominfo Location Enrich Inputs
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: lookup-outputfields-location-enrich
      path: /lookup/outputfields/location/enrich
      operations:
      - name: locationenrichoutputs
        method: GET
        description: Zoominfo Location Enrich Outputs
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.ZOOMINFO_API_KEY}}'
  exposes:
  - type: rest
    namespace: zoominfo-company-location-enrich-rest
    port: 8080
    description: REST adapter for ZoomInfo — Company Location Enrich. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/enrich/location
      name: enrich-location
      description: REST surface for enrich-location.
      operations:
      - method: POST
        name: locationenrich
        description: Zoominfo Location Enrich
        call: zoominfo-company-location-enrich.locationenrich
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/lookup/inputfields/location/enrich
      name: lookup-inputfields-location-enrich
      description: REST surface for lookup-inputfields-location-enrich.
      operations:
      - method: GET
        name: locationenrichinputs
        description: Zoominfo Location Enrich Inputs
        call: zoominfo-company-location-enrich.locationenrichinputs
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/lookup/outputfields/location/enrich
      name: lookup-outputfields-location-enrich
      description: REST surface for lookup-outputfields-location-enrich.
      operations:
      - method: GET
        name: locationenrichoutputs
        description: Zoominfo Location Enrich Outputs
        call: zoominfo-company-location-enrich.locationenrichoutputs
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: zoominfo-company-location-enrich-mcp
    port: 9090
    transport: http
    description: MCP adapter for ZoomInfo — Company Location Enrich. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: zoominfo-location-enrich
      description: Zoominfo Location Enrich
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: zoominfo-company-location-enrich.locationenrich
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: zoominfo-location-enrich-inputs
      description: Zoominfo Location Enrich Inputs
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: zoominfo-company-location-enrich.locationenrichinputs
      outputParameters:
      - type: object
        mapping: $.
    - name: zoominfo-location-enrich-outputs
      description: Zoominfo Location Enrich Outputs
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: zoominfo-company-location-enrich.locationenrichoutputs
      outputParameters:
      - type: object
        mapping: $.
